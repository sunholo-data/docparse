[project]
name = "docparse-benchmarks"
version = "0.1.0"
description = "Benchmarks for DocParse document parser"
requires-python = ">=3.11"
dependencies = [
    "fpdf2>=2.8",
]

[project.optional-dependencies]
competitors = [
    "unstructured[docx,pptx,xlsx,pdf]",
]
